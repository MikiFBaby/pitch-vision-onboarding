# Parakeet TDT v3 transcription server dependencies
# NeMo ASR toolkit (includes Parakeet models)
nemo_toolkit[asr]>=2.1.0
torch>=2.1.0
torchaudio>=2.1.0

# Speaker diarization (same as WhisperX pipeline)
pyannote.audio>=3.3.0

# RunPod serverless handler
runpod>=1.6.0

# API server (for local/direct mode)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Audio processing
soundfile>=0.12.1
numpy>=1.24.0
librosa>=0.10.0
